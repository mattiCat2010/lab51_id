# WORK IN PROGRESS///

# Development Installation Guide

This guide outlines the steps for setting up a local development environment.

## Database

Instructions for running PostgreSQL locally (e.g., using `docker run` or installing directly) and seeding initial data.

## Backend

Steps to install Node.js dependencies, configure environment variables, and start the local API server.

## Frontend

Steps to install React dependencies and start the local development server for the UI.